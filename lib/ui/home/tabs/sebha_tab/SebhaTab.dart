import 'package:flutter/material.dart';

class SebhaTab extends StatelessWidget {
  const SebhaTab({super.key});
  // finish sebha assignment
  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.black);
  }
}
